Debug running on Android Phone.

Step Sequencer Working.

Piano roll working.

Generating Live Audio.

Audio out working .

GUI is very laggy on Android.

Export .mmp is saving parsed file.

Volume ADSR partially implemented.

Effects test tab.

Known Issues.
- Sounds different on phone (compared to running in QT Creator 6 in Windows)
- If W1 expression is used, parsed .mmp will crash LMMS on Windows
- Patterns are saved with sequencer's numbers, but loaded back in (same / correct sequence as saved but renamed as 1,2,3,4, 5,6,7,8 - will investigate adding tags to patterns)
